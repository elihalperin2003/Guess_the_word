import random


def main():
    corect_word = get_random_word()
    guess_word = "_" * len(corect_word)
    garbage = []
    attempts = 8
    while corect_word != guess_word:
        guess_letter = input_letter()
        check = check_letters(corect_word, guess_letter)
        pass


def get_random_word():
    words = ["a", "bb", "ccc"]
    return words[random.randint(0, len(words) - 1)]


def prinr_detales(guess_word, garbage: list, attempts):
    print()
    print("Guess the word:")
    print(guess_word)
    print(f"Garbage: {[letter for letter in garbage]}")
    print(f"Attempts left: {attempts}")


def input_letter():
    """Prompts for a single English letter, repeating until valid. Returns the input as a lowercase string."""
    right_input = False
    while not right_input:
        letter = input("enter letter: ")
        letter = letter.lower()
        if not "a" <= letter <= "z" or len(letter) != 1:
            print("Incorrect input, please type again")
            continue
        else:
            right_input = True
    return letter


def check_letters(corect_word, guess_letter):
    for letter in corect_word:
        if letter == guess_letter:
            return True
    return False
